# Python session example

Install the `aeromaint-capture-sdk` wheel, set `AEROMAINT_API_URL` and optionally `AEROMAINT_TOKEN`, then run `python session_window.py`. The same file can be pasted into a notebook cell; it imports no repository modules.
